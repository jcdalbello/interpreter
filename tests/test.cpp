#include "../include/ex_interpreter.hpp"
#include <iostream>
#include <cassert>

/*
int main()
{
    std::vector<std::string> v1 = {"1", "2", "+"};
    assert(ex_interpreter(v1) == 3);

    v1 = {"5", "1", "-"};
    assert(ex_interpreter(v1) == 4.0);

    v1 = {"1", "1", "-"};
    assert(ex_interpreter(v1) == 0.0);

    v1 = {"1", "2", "-"};
    assert(ex_interpreter(v1) == -1.0);

    v1 = {"2", "8", "*"};
    assert(ex_interpreter(v1) == 16.0);

    v1 = {"1", "0", "*"};
    assert(ex_interpreter(v1) == 0.0);

    v1 = {"10", "5", "/"};
    assert(ex_interpreter(v1) == 2.0);

    v1 = {"5", "10", "/"};
    assert(ex_interpreter(v1) == 0.5);

    v1 = {"2", "3", "^"};
    assert(ex_interpreter(v1) == 8.0);

    return 0;
}
*/
